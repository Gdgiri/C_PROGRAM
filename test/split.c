#include <stdio.h>

int main()
{
    char name[20];
    int id, amount, cash, gst, total;

    printf("Enter the Name: ");
    scanf("%s", &name);

    printf("Enter teh ID: ");
    scanf("%d", &id);

    printf("Enter the Amount: ");
    scanf("%d", &amount);
}